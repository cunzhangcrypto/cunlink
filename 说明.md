# Cunlink 部署操作说明

> 适用于：通过 GitHub Fork + Cloudflare Pages 部署的粉丝用户

---

## 一、准备工作

1. **Fork 本仓库** 到你的 GitHub
2. **Cloudflare Dashboard** → **Workers & Pages** → **创建** → **Pages** → **连接到 Git**，选择你的 fork
3. 框架选 **无**，构建命令留空，点击 **开始部署**
4. 部署成功后，进行以下绑定：

| 类型 | 变量名 | 操作 |
|------|--------|------|
| D1 数据库 | `DB` | 创建数据库 → 绑定 |
| KV 命名空间 | `SLUG_KV` | 创建命名空间 → 绑定 |

绑定路径：Worker → **设置** → **绑定** → **添加绑定**

---

## 二、初始化数据库（关键步骤）

> D1 Console 每次只能执行 **1 条 SQL**，以下每步都要复制一行 → 粘贴执行 → 成功后再执行下一条。

### 打开 D1 Console

Worker → **D1** 标签 → 点击你的数据库 → **控制台**

### 如果数据库是全新的（首次部署）

按以下顺序，逐条执行 `migrations/init.sql` 中的 SQL：

| 步骤 | 说明 | 执行几次 |
|------|------|---------|
| 第 1~7 步 | 创建 7 张数据表 | 共 7 次 |
| 第 8 步 | 插入默认设置 | 共 1 次 |
| 第 9 步 | 创建 14 个索引（每行一次） | 共 14 次 |

> **总共需要操作 22 次**，每次复制一行，粘贴到控制台，点击执行。

### 如果需要清空重建（数据库已有旧数据）

先逐条执行以下 7 条删除语句（同样每次一条）：

```sql
DROP TABLE IF EXISTS bundle_links;
DROP TABLE IF EXISTS bundles;
DROP TABLE IF EXISTS api_keys;
DROP TABLE IF EXISTS settings;
DROP TABLE IF EXISTS clicks;
DROP TABLE IF EXISTS slugs;
DROP TABLE IF EXISTS links;
```

然后再按上面的 **第 1~9 步** 顺序建表。

---

## 三、完成

所有 SQL 执行成功后，刷新你的 Pages 页面即可正常使用。
